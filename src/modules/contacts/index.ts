export { Contacts } from './Contacts';
export type { Contact, ContactsResponse, GetContactsParams } from './types';

// Loader function for React Router (if needed in the future)
export const contactLoader = () => {
  // Add any data loading logic here
  return null;
};
